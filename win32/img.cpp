#include "img.h"
#include "../str.h"
//#include <shellapi.h>

namespace win32
{
   void img::load_from_file(const std::string& path)
   {
      //HICON hIcon = ::ExtractIcon(NULL, str::to_wstr(path).c_str(), 0);

   }
}